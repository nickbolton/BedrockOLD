#include "Pods-Bedrock.xcconfig"
GCC_PREPROCESSOR_DEFINITIONS = COCOAPODS=1
HEADER_SEARCH_PATHS = "${PODS_ROOT}/BuildHeaders" "${PODS_ROOT}/BuildHeaders/Bedrock" "${PODS_ROOT}/Headers" "${PODS_ROOT}/Headers/Bedrock"
OTHER_LDFLAGS = -ObjC
PODS_ROOT = ${SRCROOT}